---
id: 076
area: montrose
type: signoff
turns: 120
title: Montrose sign-off (§5.2) — formalize the stretch, unlock the planner
acceptance: >
  Run the full §5.2 sign-off on the montrose area in ONE fresh walkthrough
  (node tools/walkthrough.mjs --area montrose): (1) every mt-* waypoint has
  an authored expectation and every expectation is judged MET from the PNGs
  with a per-waypoint verdict logged — every PNG personally Read; (2) the
  standing gate green including draw calls ≤480 at every mt waypoint;
  (3) walkprobe covers every new walkable surface (trail extension, harbor
  pieces, the hook, the Point, the beach + dunes edges, the hill slope +
  summit) and exits 0; (4) at least 3 delight moments shipped and logged in
  DELIGHT-SHIPPED.md for this area (074's ledger lines verified present at
  their commits); (5) the EVOCATION REVIEW passes: a fresh-eyes subagent
  that has NOT seen the build tasks Reads the contact sheet against
  refs/montrose/ and must name where it is and cite what gives it away —
  a wrong or vague answer FAILS the review; file polish tasks, renumber
  this task after them per the iteration script, and stop honestly.
  (6) Write refs/montrose/SIGNOFF.md (per-waypoint verdicts + contact-sheet
  path + the evocation transcript), update LOCATIONS.md (Montrose →
  Shipped, with the sign-off date and any standing liberties worth
  surfacing), commit, push. Only a green sign-off unlocks planning the
  next location. Also verify the CONTIGUITY promise held: one uncut
  walk/ride from the Belmont Rocks to Montrose Beach with no seam, and the
  minimap reads the full tall map sanely.
refs:
  - AUTOPILOT.md §5.2 (the sign-off contract — follow it mechanically)
  - refs/montrose/ (BRIEF.md, refs, osm.json), refs/millennium-park/
    SIGNOFF.md + refs/wrigleyville/SIGNOFF.md (format precedents)
  - DELIGHT-SHIPPED.md, autopilot/issues/ (must be montrose-clean)
---

The gate exists to catch wishful green — judge the walkthrough as if
someone else built it. The evocation reviewer is the owner's proxy: if it
can't say "Montrose — the hedge, the hook, Cricket Hill" unprompted, the
stretch isn't done, no matter how green the mechanics are.
