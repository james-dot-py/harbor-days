---
id: 071
area: montrose
type: build
model: fable
turns: 120
title: THE MAGIC HEDGE — Montrose Point bird sanctuary (the signature-landmark hero build)
acceptance: >
  The most famous birding spot in the Midwest reads instantly, in house toon
  style, and is a place you want to linger: (1) MONTROSE POINT: the
  peninsula's coast piece(s) replace their 069 stubs (own deterministic
  pieces, walkable top per shared data); meadow + prairie-grass carpet, tree
  clusters per refs, winding crushed-limestone sanctuary paths as NEW
  ribbons via pathSamples2. (2) THE HEDGE ITSELF: a long dense hedgerow
  running the Point per the osm/refs geometry — reads as a green WALL with
  gaps birders peer through, built from existing instanced shrub/tuft
  buckets + merged statics (zero new InstancedMesh buckets, the 068 law);
  the real brown 'MONTROSE POINT BIRD SANCTUARY — The Magic Hedge' sign at
  the entrance (own canvas, measureText-fitted, FrontSide + solid rear —
  the word-sign laws). (3) BIRDING LIFE: birder NPCs with tripod scopes and
  binoculars (makeNPC, ope lines in the birder register — "big year",
  "lifer", "it's the warblers"), and CHIBI-CHUNKY birds CLUSTERED into the
  focal clearings the waypoints frame (BIRD_SCALE ~2.5, a couple seated
  eye-level on near perches — true-scale scatter never reads, PITFALLS);
  reuse/extend the nature-pack bird machinery rather than inventing a
  parallel system, distance-culled with the z-sign checked against
  GEOGRAPHY (the task-025 cull-sign pitfall). (4) REGISTER: distinct from
  Jarvis — open sky, lake horizon behind the hedge, scopes-and-rarities
  energy, no bingo duplication; if the Point grades ambience (boosted
  birdsong via the definePlace pattern) it must release cleanly at the
  boundary. (5) DETERMINISM: canonical spawn shot ≈noise vs baseline; all
  scatter local-seeded; no shared rng. (6) WAYPOINTS: mt-hedge (the
  hedgerow + birders read), mt-point (meadow toward open lake), plus the
  BRIEF's remaining Point strings, wired + expects; walkthrough green,
  draws ≤480 everywhere checked, every PNG personally Read and judged vs
  expectation + refs + the art-director bar. (7) walkprobe exit 0; `npm
  run build` one artifact; zero console/page errors; canary echoes.
refs:
  - refs/montrose/BRIEF.md + osm.json + fetched hedge/sign/birder imagery
  - GEOGRAPHY.md Montrose section (068) + the Jarvis sanctuary section (the
    register to be DISTINCT from)
  - src/packs/nature.js or equivalent (existing bird/binocular machinery —
    grep before building), src/character.js (chibi NPCs)
  - PITFALLS.md (chibi-chunky birds; cull sign; word-sign laws; pathSamples2)
---

Hero task — this is the landmark the owner named. The evocation bar: a
Chicago birder must look at mt-hedge and say "that's the Magic Hedge" from
the hedgerow-plus-scopes-plus-open-lake composition alone. Taste guidance:
the magic is the CONTRAST — an unassuming line of shrubs that happens to be
mobbed with life and devotees; sell it with density AT the hedge (birds,
birders, a rarities buzz) against the Point's calm open meadow. The Nike
missile-site history is a plausible one-line sign or NPC aside, not a
build-out.
