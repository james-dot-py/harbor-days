# Ruby tooling for the iOS → TestFlight rail (task 083). Used ONLY by the
# macOS CI workflow (.github/workflows/ios-testflight.yml); the game itself has
# no Ruby. fastlane drives the archive + App Store Connect upload.
source "https://rubygems.org"

gem "fastlane"
