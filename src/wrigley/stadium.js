// WRIGLEY FIELD — the hero. Facade ring, gates, marquee, light towers,
// interior bowl (field/ivy/bleachers), scoreboard, knothole. Data: STADIUM_W.
// Fable-owned. Attach to wrigleyRoot; wrand only.
import { wrigleyRoot, wrand } from './index.js';

export function buildStadium() {
  // stub — Fable builds this in Wave 1
}
export function marqueeSetText(line) { /* wired with the marquee build */ }
export function raiseW() { /* wired with the scoreboard build */ }
