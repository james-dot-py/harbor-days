// Wrigleyville streets — sidewalks/curbs/markings, lamps, blade signs,
// CPD barricades, Lakeview backdrop band. Data: ../data/wrigleyville.js.
// Attach everything to wrigleyRoot. Local randomness only (wrand).
import { wrigleyRoot, wrand } from './index.js';

export function buildStreets() {
  // stub — Wave 1 executor builds this (exclusive file)
}
